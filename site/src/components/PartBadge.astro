---
// Part-number chip (e.g. "Part 1" of R 60-1:2021).
interface Props { partNumber: string }
const { partNumber } = Astro.props
---

<span class="px-2 py-0.5 text-xs font-mono uppercase border border-rule rounded-sm">Part {partNumber}</span>
